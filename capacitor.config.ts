import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sistemaMusica-apk',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
